import ReflectionPage from '@/components/reflection/ReflectionPage';

export const metadata = {
  title: 'AI Reflection — FiinQuant AI',
};

export default function ReflectionRoute() {
  return <ReflectionPage />;
}
