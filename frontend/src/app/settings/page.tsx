import SettingsPage from '@/components/settings/SettingsPage';

export const metadata = {
  title: 'Cài đặt — FiinQuant',
  description: 'Quản lý tài khoản, cảnh báo giá và thông báo AI',
};

export default function SettingsRoute() {
  return <SettingsPage />;
}
