import WhatIfSimulatorPage from '@/components/analysis/WhatIfSimulatorPage';

export const metadata = {
  title: 'Simulator — FiinQuant',
  description: 'Trình giả lập danh mục: thử nghiệm kịch bản và xem tác động',
};

export default function SimulatorRoute() {
  return <WhatIfSimulatorPage />;
}
